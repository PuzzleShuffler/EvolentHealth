# EvolentHealth
 
Analyzing beer styles with `pandas`. Includes `sentiment analysis` using `NLTK` of review text to find polarity scores, how positive or negative the reviews are, to find the best and worst beer styles. Also includes `random forest regressor` to see which factors affect the overall score the most. Report is within `EvolentHealth.ipynb`.